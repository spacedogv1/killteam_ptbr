name: Publicar no GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Baixar o projeto
        uses: actions/checkout@v4

      - name: Configurar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Instalar dependências
        working-directory: app
        run: pnpm install --frozen-lockfile

      - name: Gerar versão do site
        working-directory: app
        env:
          GITHUB_PAGES: 'true'
        run: pnpm run build -- --prerender-all

      - name: Preparar arquivos publicados
        shell: bash
        run: |
          rm -rf _site
          mkdir -p _site
          cp -R app/dist/client/. _site/
          mkdir -p _site/killteam_ptbr
          cp -R app/dist/client/_next _site/killteam_ptbr/_next
          cp -R app/dist/client/data _site/killteam_ptbr/
          cp app/dist/client/favicon.svg _site/killteam_ptbr/favicon.svg
          cp app/dist/client/manifest.webmanifest _site/killteam_ptbr/manifest.webmanifest
          cp app/dist/client/sw.js _site/killteam_ptbr/sw.js
          cp app/dist/server/prerendered-routes/index.html _site/index.html
          cp app/dist/server/prerendered-routes/404.html _site/404.html
          sed -i 's|/_next/|/killteam_ptbr/_next/|g' _site/index.html _site/404.html
          touch _site/.nojekyll

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Enviar arquivos para o Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Publicar
        id: deployment
        uses: actions/deploy-pages@v4
